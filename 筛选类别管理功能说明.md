# 商品筛选类别管理功能说明

## 功能概述

为小程序商品页面的分类按钮功能开发了完整的后台管理模块，支持类别信息的增删改查、批量操作、数据导入导出，并与前台实时联动。

## 文件清单

### 后台模型层
1. **apps/sp_mall/sp_filter_category_models.py** - 筛选类别和操作日志数据模型
2. **apps/sp_mall/sp_filter_category_api.py** - RESTful API接口

### 后台管理界面
3. **apps/templates/sp_mall_admin/sp_filter_category_list.html** - 类别管理HTML页面
4. **apps/sp_mall_admin/filter_category_routes.py** - 管理页面路由

### 小程序端
5. **utils/sp_api.js** - 添加筛选类别API调用
6. **pages/Product_list_page/Product_list_page.js** - 动态加载筛选类别

### 配置更新
7. **apps/sp_mall/register.py** - 注册筛选类别API
8. **apps/__init__.py** - 注册蓝点和API
9. **apps/sp_mall_admin/__init__.py** - 导出新蓝图
10. **templates/includes/sidebar.html** - 添加导航菜单

## 核心功能

### 1. 类别管理 (CRUD)
- ✅ 创建新类别（名称、编码、排序、状态、图标、颜色、描述）
- ✅ 编辑已有类别
- ✅ 删除类别
- ✅ 查看类别详情

### 2. 状态管理
- ✅ 启用/禁用类别
- ✅ 批量启用/禁用
- ✅ 排序权重调整

### 3. 批量操作
- ✅ 批量选择类别
- ✅ 批量启用/禁用
- ✅ 批量删除

### 4. 数据导入导出
- ✅ 导出所有类别为JSON格式
- ✅ 从JSON文件导入类别数据
- ✅ 导入预览功能
- ✅ 自动识别新增/更新

### 5. 操作日志
- ✅ 记录所有操作（增删改、批量操作、导入导出）
- ✅ 记录操作人信息
- ✅ 记录操作时间
- ✅ 记录操作前后数据变化

### 6. 权限控制
- ✅ 管理员身份验证
- ✅ 操作者IP记录
- ✅ 操作人追踪

### 7. 前端实时联动
- ✅ 小程序自动从API获取类别列表
- ✅ 无需更新小程序即可变更筛选类别
- ✅ 支持"全部"选项

## API接口

### 小程序端API
```
GET /api/sp/filter_category/list?status=1
```
返回启用的筛选类别列表

### 管理端API
```
GET    /api/sp/filter_category              - 获取类别列表（分页）
POST   /api/sp/filter_category               - 创建类别
GET    /api/sp/filter_category/:id           - 获取类别详情
PUT    /api/sp/filter_category/:id          - 更新类别
DELETE /api/sp/filter_category/:id         - 删除类别
POST   /api/sp/filter_category/toggle-status/:id - 切换状态
POST   /api/sp/filter_category/batch-update - 批量更新
GET    /api/sp/filter_category/export       - 导出数据
POST   /api/sp/filter_category/import       - 导入数据
GET    /api/sp/filter_category/logs         - 操作日志
```

## 数据库表结构

### sp_filter_category (筛选类别表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 类别ID |
| name | VARCHAR(50) | 类别名称 |
| code | VARCHAR(50) | 类别编码（唯一） |
| sort | INT | 排序权重 |
| status | SMALLINT | 状态：1启用 0禁用 |
| icon | VARCHAR(500) | 类别图标 |
| color | VARCHAR(20) | 主题颜色 |
| description | VARCHAR(200) | 类别描述 |
| product_count | INT | 商品数量 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### sp_category_operation_log (操作日志表)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 日志ID |
| category_id | INT | 操作的类别ID |
| operation_type | VARCHAR(20) | 操作类型 |
| operator_id | BIGINT | 操作人ID |
| operator_name | VARCHAR(100) | 操作人名称 |
| operator_ip | VARCHAR(50) | 操作人IP |
| old_data | TEXT | 操作前数据 |
| new_data | TEXT | 操作后数据 |
| description | VARCHAR(500) | 操作描述 |
| status | SMALLINT | 状态：1成功 0失败 |
| error_message | TEXT | 错误信息 |
| created_at | DATETIME | 操作时间 |

## 使用说明

### 后台管理
1. 访问 http://localhost:5000/admin/sp/filter-category
2. 或通过侧边栏：商品订单管理 → 筛选类别管理

### 添加类别
1. 点击"添加类别"按钮
2. 填写类别名称（必填）
3. 填写类别编码（必填，唯一）
4. 设置排序权重（数字越大越靠前）
5. 选择状态（启用/禁用）
6. 可选：设置图标、颜色、描述
7. 点击保存

### 编辑类别
1. 点击类别卡片的编辑按钮
2. 修改需要更新的字段
3. 点击保存

### 批量操作
1. 勾选要操作的类别
2. 在底部操作栏选择操作类型
3. 确认执行

### 导入导出
1. **导出**：点击"导出"按钮，自动下载JSON文件
2. **导入**：点击"导入"按钮，选择JSON文件，预览后确认导入

### 前台联动
1. 在后台添加/编辑/删除类别后
2. 小程序商品页面会自动显示最新类别
3. 无需更新小程序代码

## 数据格式

### 导出JSON格式
```json
{
  "exportTime": "2026-04-02 10:00:00",
  "total": 6,
  "categories": [
    {
      "id": 1,
      "name": "护肤",
      "code": "skincare",
      "sort": 100,
      "status": 1,
      "icon": "feather icon-heart",
      "color": "#667eea",
      "description": "护肤产品分类",
      "productCount": 0
    }
  ]
}
```

### 导入JSON格式
```json
{
  "categories": [
    {
      "name": "护肤",
      "code": "skincare",
      "sort": 100,
      "status": 1,
      "icon": "feather icon-heart",
      "color": "#667eea",
      "description": "护肤产品分类"
    }
  ]
}
```

## 注意事项

1. **编码唯一性**：类别编码必须唯一，重复会导致导入失败
2. **状态联动**：禁用的类别不会在小程序中显示
3. **实时生效**：所有修改会立即生效，无需重启服务
4. **日志保留**：操作日志会永久保留，便于审计追踪
5. **数据安全**：删除操作不可恢复，请谨慎操作

## 权限说明

当前版本权限控制基于Flask-Login的登录状态：
- 所有登录用户都可以访问管理界面
- 操作日志会记录当前登录用户信息
- 如需更严格的权限控制，可在路由层添加权限装饰器

## 故障排查

### 类别不显示
1. 检查类别状态是否为"启用"
2. 检查API响应是否正常
3. 查看控制台是否有错误信息

### 导入失败
1. 检查JSON格式是否正确
2. 检查是否有重复的类别编码
3. 查看错误提示信息

### 操作无响应
1. 检查网络连接
2. 查看浏览器控制台错误
3. 检查Flask服务日志
